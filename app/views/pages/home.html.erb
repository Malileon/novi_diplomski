<main class="container">
  <h1 class="home__center home__title">Quiz page</h1>

  <div class="home__center">
    <div class="home__title">
      A quiz page with <%= Quiz.all.count %> quizzes<br>
      in <%= Topic.all.count %> topics.
    </div>
    <%= link_to "View topics", topics_path, class: "btn btn--dark" %>
    <%= link_to "Sign in", new_user_session_path, class: "btn btn--dark" %>
  </div>
</main>